# MySQL database package

Paket ini menyediakan connection pool `database/sql`, transaksi, query rows, konversi placeholder terindeks, dan kompatibilitas hasil `RETURNING` untuk repository aplikasi di atas driver `go-sql-driver/mysql`.
